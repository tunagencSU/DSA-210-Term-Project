First, save all the code and files exactly as they are into a single directory. 
Once the files are saved, run the `app.py` file from your terminal.
After running the file, there will be a link poping up in the terminal. From that link
you can accsess to the local website
